import { DragSlider } from "./drag-slider";

const dragslider = new DragSlider('#drag-slider', { gap: 12, transition: true});

 const dragslider2 = new DragSlider('#chip', { threshold: 1, gap: 20, transition: false});
